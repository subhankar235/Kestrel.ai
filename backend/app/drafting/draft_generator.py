"""Draft generator — generates 3 distinct candidate angles per accepted topic."""

from __future__ import annotations

from typing import Any


async def generate_draft_angles(topic: dict[str, Any], persona: dict[str, Any]) -> list[dict[str, Any]]:
    """Generate 3 candidate post drafts with distinct angles."""
    return []
